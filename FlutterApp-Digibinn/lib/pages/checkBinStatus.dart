import 'package:flutter/material.dart';

class BinStatus extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          
          title: Text('Check Bin Status'),
          centerTitle: true,
          backgroundColor: Colors.green,
        ),
      );
}